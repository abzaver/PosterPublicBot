{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PosterBot",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/pubBot.py",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal"
        }
    ]
}
